# Auction Assets

Create a folder for each item using its `item_id`.

Example:

```
auction/assets/item1/1.jpg
auction/assets/item1/2.jpg
auction/assets/item1/3.jpg
```

The item page attempts to load `1.jpg` through `6.jpg` for each item and only shows the images that exist.
